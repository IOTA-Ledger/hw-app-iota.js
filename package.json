{
  "name": "hw-app-iota",
  "version": "0.6.1",
  "scripts": {
    "clean": "rimraf dist",
    "format": "prettier --single-quote --write src/**/*.js test/**/*.js",
    "lint": "eslint src",
    "build": "babel src -d dist",
    "test": "npm run build && mocha --require babel-polyfill",
    "prepublishOnly": "npm run clean && npm run lint && npm run test && npm run build"
  },
  "description": "Ledger Hardware Wallet IOTA Application API",
  "main": "dist/iota.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IOTA-Ledger/hw-app-iota.js.git"
  },
  "keywords": [
    "Ledger",
    "LedgerWallet",
    "IOTA",
    "NanoS",
    "Hardware",
    "Wallet"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "bip32-path": "^0.4.2",
    "iota.lib.js": "^0.5.2",
    "semver": "^6.1.0",
    "struct": "0.0.12"
  },
  "devDependencies": {
    "@iota/checksum": "^1.0.0-beta.11",
    "@iota/core": "^1.0.0-beta.11",
    "@ledgerhq/hw-transport-mocker": "^4.60.2",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.2.0",
    "eslint": "^5.16.0",
    "mocha": "^6.1.4",
    "prettier": "^1.17.1",
    "rimraf": "^2.6.3"
  }
}
